{% if download %}
    <a class="c-button" href="{{ download|url }}" download>
      {{ ":title (:type, :size)"|trans({
        title: 'Beschreibung als PDF herunterladen',
        type: download|file_extension|upper,
        size: download|file_size
      }) }}
    </a>
  {% endif %}
