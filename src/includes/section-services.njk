{% set services = sections[lang].services|default([]) %}

{% if services|length %}
  <section class="c-section c-section--flush c-section--services">
    <h2 class="c-section__title / u-sr-only">
      {{ "Merchandise presentation"|trans }}
    </h2>

    {% for item in services %}
      {% include "billboard.njk" %}
    {% endfor %}
  </section>
{% endif %}
